package com.taxonomy.shared.extension;

/**
 * Stable categories for internal taxonomy extension points.
 */
public enum ExtensionKind {
    EXPORT_FORMAT,
    REPORT_RENDERER,
    IMPORT_PROFILE,
    LLM_PROVIDER,
    ARCHITECTURE_PIPELINE_STEP
}
