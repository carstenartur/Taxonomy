# Visio graphical quality implementation plan

Design: [finalized specification](../specs/2026-09-20-visio-quality.md).

1. Add failing numerical tests for reverse/vertical connector text transforms, caption/node and caption/caption collision, stroke layering, dense relation coverage and Unicode display bounds.
2. Extend the existing export owner with full cached/formula text transforms, deterministic bounded caption placement and native relationship detail pages. Reserve complete overview and impact pages within all global limits. Keep exact canonical properties and dynamic glue; add no architecture-module dependency.
3. Exercise exact page and near-byte boundaries, long Unicode, coincident/multiple/reverse edges, deterministic membership and native endpoint identity. Run affected package/schema/POI/handoff/application regressions; regenerate the intentional reproducible fixture and checksums.
4. Extend the existing civilian Draw/Poppler checker to verify canonical and rendered relationship key/direction/type coverage. Add a focused Visio-only mode while retaining the standard complete document path. Generate actual HTTP saved-snapshot exports with the existing civilian acceptance scenario, render every changed VSDX page and retain source/artifact hashes, renderer version and machine-readable coverage. Do not repeat the unchanged Word visual suite.
5. Update EN/DE user documentation and QA dispositions. Review readable source, focused covering tests and diff checks, commit the task and release source/Maven ownership for independent review and exhaustive all-page inspection. Microsoft Visio and EA/PCS remain NOT_EXECUTED.

This work is independent of conditional publication: it consumes accepted native/Word/frozen civilian graph inputs. Publication review may run read-only concurrently; exactly one implementer owns source and Maven. The controller owns combined gates and merges.
