name: Temporary merge-base source export

on:
  pull_request:
    branches: [ main ]

permissions:
  contents: read

jobs:
  export:
    if: github.head_ref == 'temp/base-002c-source-export'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false
      - name: Stage source snapshot
        run: |
          set -euo pipefail
          mkdir -p target/base-source
          rsync -a --delete \
            --exclude '.git/' \
            --exclude 'target/' \
            --exclude '*/target/' \
            --exclude 'node_modules/' \
            ./ target/base-source/
      - uses: actions/upload-artifact@v4
        with:
          name: taxonomy-base-source
          path: target/base-source
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 1
