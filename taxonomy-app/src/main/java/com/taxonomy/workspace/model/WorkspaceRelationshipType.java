package com.taxonomy.workspace.model;

/** Provenance relationship between a workspace and its source repository. */
public enum WorkspaceRelationshipType {
    WORKING_COPY,
    FORK_WORKSPACE,
    INDEPENDENT
}
