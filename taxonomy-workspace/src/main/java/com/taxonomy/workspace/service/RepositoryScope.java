package com.taxonomy.workspace.service;

/** Operation scope for an explicitly selected architecture repository context. */
public enum RepositoryScope {
    CENTRAL_READ,
    CENTRAL_WRITE,
    WORKSPACE,
    FORK
}
