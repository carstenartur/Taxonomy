# Completion decision register — 2026-09-20

The entries preserve substantive decisions, rationale and risks from the completion
index and owning Word, native, publication and Visio ledgers. Decision order is
retained within each workstream; interleaved cross-slice clarifications follow.
Current accepted evidence is in [1075-completion-evidence.md](1075-completion-evidence.md).
Historical pending verdicts below describe the decision when recorded, not current status.

Superseded decisions remain visible: the old 65-page Word ceiling became 74 after
measured output, with 12 for the standalone report; the initial ordinary-merge
proposal yielded to the supported squash workflow; Visio's earlier “aggregate XML”
shorthand was corrected to **all uncompressed package parts including the manifest**.
Earlier pending Word/native/Visio review gates are superseded by the accepted evidence.
Real proprietary-product acceptance remains unexecuted. No raw logs, prompts or
credentials are reproduced here.

## D01 — Frozen Word

Proceed without another plan approval pause — user has repeatedly directed completion and merge; higher-priority autonomy instructions apply — a design mistake is reviewable and reversible.

## D02 — Frozen Word

Use one implementer for the complete Word deliverable — routes/templates/source model must be reviewed together and no parallel Maven reactors are safe — task is larger but has one independently testable outcome.

## D03 — Frozen Word

Workbench graph is frozen, but fallback titles use current project/requirement labels — document composition must use saved decision/version text for report evidence and ignore mutable display-only titles where necessary — provenance mismatch must not be hidden.

## D04 — Frozen Word

Preserve Java 21 compiler/API target — JDK 25 belongs only to the local verification runtime — new API usage must compile and run with CI21.

## D05 — Frozen Word

Include the workbench Word control and tooling render of both Word files — a new API route alone does not complete the user's report workflow — same implementation owns these source/output consumers and records actual browser/render gates.

## D06 — Frozen Word

Raise the decision document ceiling from 65 to 74 after corrected real output measured 71 pages — the required architecture/inventories and whole tree add sections that were absent from the old budget; allow three pages of renderer variance while retaining reading-scale/orphan/clipping gates — too small a variance could require a later justified adjustment, too large could hide pagination regression. Standalone architecture budget will be set only after its own measured output.

## D07 — Frozen Word

Set the standalone architecture document ceiling to 12 after its real output measured 10 pages — separate small renderer variance is appropriate to this shorter report — more than two pages of renderer drift would require new measured evidence, while the ceiling still catches runaway pagination.

## D08 — Frozen Word

Publish three cohesive PR slices (Word, semantic adapter/native mapping, conditional publication) against exact tested trees; merge after final whole-branch review and green gates — this avoids an oversized combined PR, with the cost of coordinating dependent branches.

## D09 — Frozen Word

Retain frozen node labels in canonical graph content digest; exclude mutable report/scene presentation titles. Copilot correctly identified an overbroad class comment, but removing saved labels would weaken evidence integrity and is not required by the actual frozen-source contract. Clarify source/spec and cover both cases. Cost if wrong: consumers expecting name-insensitive content hashes would need an explicit separate identity digest, not a silent algorithm change.

## D10 — Frozen Word

Accept four exact Word dependency-count changes after source review — planned architecture-owned report composition and typed layout-error boundary, existing directions/no reverse cycle — cost if wrong: hiding unnecessary coupling in the baseline; independent scoped review required.

## D11 — Frozen Word

Finish the independent Word plan with its own broad final review and single combined final-quality fix wave on the stable isolated Word branch now; the later broad native/publication review starts from that accepted Word foundation instead of re-reviewing unchanged Word code — Word's only task and all scoped fixes are accepted, and the user explicitly wants green PRs fixed and merged; waiting for three unrelated publication tasks delays an independently mergeable result — cross-feature effects could arise later, so final combined root/CI gates and frozen-report contracts remain mandatory after native/publication changes. All deferred Word minors remain in scope; known VSDX graphical-quality triage stays in the remaining overall completion scope.

## D12 — Frozen Word

Final reviewer declined-to-judge dispositions — proprietary Word compatibility stays unexecuted; VSDX presentation defect remains overall completion task, not approved; native/publication readiness remains their own gates; pending CI and policy-skipped CodeQL remain controller-owned; pre-existing warning cleanup stays nonblocking because no connected Word failure was established; administrator/source text remains unchanged per explicit contract; adding language selection to legacy live DTO is outside bilingual snapshot requirement; byte-identical generatedZIPs are not required when volatilemetadata differs but savedsource/graphparity is enforced — these preserve actual scope/evidence — if wrong, undiscovered viewer/font/otherexport behavior must be caught by the remaining specific gate rather than claimed certified.

## D13 — Frozen Word

retain existing duplicate-anchor-name rejection while expanding reservation to all note stories — silently renaming administrator links would change template meaning — a custom template that owns a required generated anchor must rename that anchor.

## D14 — Frozen Word

Word merge gate uses the exact-tree complete CI reactor, all database profiles, real browser and document workflows after the final wave; final combined local clean-root gate runs once after all implementation tasks — avoids duplicate full builds while preserving broader executed CI gates — local-only environment behavior is finally checked on combined code rather than per slice.

## D15 — Native semantics

Use `(connector ID, version)` registry rather than version embedded in connector ID — existing profile semantics and explicit versioning must remain clear — requires updating all dispatch and UI selection together.

## D16 — Native semantics

Native package projection is v2-only for Sparx connections — v1 durable meaning must not change automatically — users select a new/migrated version explicitly.

## D17 — Native semantics

Pure requirement planning belongs with integration domain data unless portfolio-owned inputs demand a port method — avoid placing adapter state in portfolio contract — execute under existing project lock and same transaction.

## D18 — Native semantics

Keep the real portfolio canonical requirement identity and exact key-pair validation; reject ambiguous IDs — contributor already emits a canonical requirement, while global ID migration would alter unrelated stored references — supported unambiguous mappings work and collisions fail visibly before mutation.

## D19 — Native semantics

Use atomic final-state package placement, not sequential position moves — reviewed swaps must not fail on a transient duplicate rank — complete touched-parent scope and explicit detach add payload detail but preserve all unrelated siblings.

## D20 — Native semantics

Contribute real portfolio DSL before mapping commands within the existing transaction — newly created requirement endpoints must exist without fabricated forward references — an integration-specific ordered contribution seam avoids broad editor behavior changes.

## D21 — Native semantics

After the two named owner/root-endpoint boundary cases, run exact final Task 1 codec/reader/registry/retry/app-flow covering tests and commit/report for independent task review; do not repeat the full owning reactor solely for known .rsync-tmp discovery/Word baseline failures — both full attempts were performed and retained, broader gate remains explicitly incomplete; repeated unrelated infrastructure failures do not justify redoing every unaffected test after each focused change — an unrelated regression may be discovered later at the mandatory final whole-root gate; no green full-reactor claim or exclusions permitted.

## D22 — Native semantics

Final clean whole-root verification will use a detached exact-commit Git worktree under /tmp, outside the synchronized workspace — earlier runs discovered invalid .rsync-tmp classes and the verification wrapper honors cwd — this changes only build location, keeps every test and gate unchanged; if it does not avoid the environment race, retain the failure and reassess. No exclusions will be added preemptively.

## D23 — Native semantics

Restore a verified exact-version compiler outside synchronized scratch, using existing authorized CI dependency/artifact capabilities because ordinary direct download was unavailable — maintain Java 21 target and all verification gates without weakening tests — if archive/runtime differs, checksum and compiler smoke detect it; productioncompatibility still comes from realJava 21CI.

## D24 — Native semantics

empty-string package parent represents root, null plus -1 detaches ELEMENT only — keeps explicit root distinct from detach without enumerating unrelated taxonomy roots — incorrect scope design would require package operation migration.

## D25 — Native semantics

reviewed endpoint IDs must equal normalized external connector identities, not arbitrary same-kind targets — preserves export and common-base semantics — intentional retarget needs a future explicit provenance rewrite.

## D26 — Native semantics

accept only composition.interop → interop existing dependency count 8→9 for RequirementApplyPlan — intended port-owned pure planning boundary — mistaken acceptance could conceal coupling.

## D27 — Native semantics

projection approval comes only from scoped local mapping/review authority and exact unchanged normalized ends — imported tags cannot self-authorize — strict identity checks can require a new explicit review after changed external ends.

## D28 — Native semantics

reject conflicting concurrently explicit endpoint and mapping types before writes, otherwise persist one effective normalized relation type — prevents native/export disagreement without silent priority — conflicting reviews require corrected resubmission.

## D29 — Native semantics

Native plan receives its own final broad review after these scoped CI fixes, covering its deferred minors and changed integration boundaries. Once exact-tree CI and that review pass, merge1089 with repository-supported squash; later publication/Visio review uses accepted native foundation plus new interactions — independently deliverable native work should not wait for unrelated remaining implementation — final combined clean reactor/CI still guards interaction risk.

## D30 — Native semantics

add narrow actual requirement-mapping cross-JVM replay evidence in the native final fix wave because binding spec line376 requires package and requirement-mapping idempotence across restart; the previous process fixture only asserted packages/elements. Publication Task 3 still owns distributed HTTP restart coverage. Cost if wrong: extra focused test maintenance/runtime, bounded by reusing existing fixtures and not adding a second persistence authority.

## D31 — Native semantics

resolve the reviewer screenshot cannot-verify item by the existing cross-plan ownership: native Task 2 step5 requires actual EN/DE/keyboard browser execution, which remains a final-head CI gate; the dedicated native/package and publication screenshot documentation is explicitly owned by publication Task 3 and remains a mandatory overall-completion gate. The native specification/plan does not require a separate screenshot deliverable before this independently reviewable semantic PR can merge. Reason: complete the already agreed staged delivery without pretending absent screenshots exist or adding an unplanned second native fix wave. Cost if wrong: later visual inspection may reveal a layout issue requiring correction before overall completion; no dedicated native screenshot/visual certification is claimed now.

## D32 — Native semantics

pin only the ambient heap-sample boundary for the synchronous normal-load endpoint and explicitly exercise emergency-pressure response with the real guard/endpoint — an uncontrolled environment assumption caused a demonstrated flake — cost if wrong: insufficient thread scoping could miss async behavior; keep this correction confined to synchronous cases and preserve all existing guard tests. This new execution defect receives its own narrow review, not a repeated native broad audit.

## D33 — Conditional publication

Separate OBSERVATION and COMMON checkpoint pointers — current inbound mappings deliberately have distinct internal/external baselines and do not prove convergence — existing rows migrate as observation only.

## D34 — Conditional publication

Preserve old weak publish SPI but never execute it from the new engine — source compatibility without accepting insufficient recovery semantics — adapters must implement the verified conditional contract.

## D35 — Conditional publication

Test-only HTTP contract provider has real durable CAS and key/receipt persistence — sufficient to verify reusable engine without inventing PCS semantics — live PCS writes remain fail-closed and product evidence unexecuted.

## D36 — Conditional publication

Finite invocation deadline returns durable progress; unknown outcome never releases reservation — correctness across distributed response loss outranks instant completion — expired receipt histories may require external recovery.

## D37 — Conditional publication

Support valid bounded mapping decisions frozen before effects; keep server-assigned identity remapping and post-create payload changes excluded — the existing spec distinguishes these, so a blanket all-mappings exclusion is not an acceptable substitute — if the shared mapping validation is insufficient, unsupported exact cases must fail explicitly rather than acquire native authority or silently coerce content.

## D38 — Conditional publication

Task 2 may add an immutable server-owned exact connector/profile-version neutral projection registration to the existing IntegrationDomainAdapter, retaining current Sparx-v2 defaults and static compatibility helpers; instance callers use configured projection. Registration is separate from verified publication authorization, rejects ambiguity, and cannot come from requests or imported evidence. This is the smallest boundary allowing the distinct genuine HTTP contract provider to exercise actual native Sync without impersonating Sparx or adding a second synchronization authority. Cost if wrong: another connector could unintentionally acquire native semantics; exact identity/version/default-denial tests and actual native Sync are required.

## D39 — Conditional publication

Task 3 final all-page Word verification reuses the accepted unchanged foundation and retained complete visual evidence; any actual rendering/fixture change triggers fresh relevant visual acceptance. Required final source/CI gates remain. This follows the requirement to test concrete risks rather than repeat unchanged suites. Cost if wrong: an unrecognized rendering dependency could carry stale visual evidence, so source/dependency changes must be checked before reuse.

## D40 — Conditional publication

retain already authored scoped implementation after this explicitly recorded process deviation. Corrected-fixture execution against a temporary compile stub supplies an honest missing-engine baseline, then restore the exact hashed implementation and verify real fault/Green outcomes. Do not describe this as retroactive RED-first development. Every Maven process must finish before seam/source changes; final hashes must exclude stale stub bytecode. Newly uncovered behavior changes still get meaningful failing tests first. Reason: deleting/retyping adds no verification value. Cost if wrong: post-hoc tests may share implementation blind spots; independent actual-provider/state assertions, required fault coverage and an explicit reviewer handoff are mandatory.

## D41 — Conditional publication

conditionally permit only interop.persistence→workspace.service2→3, interop.publication→workspace.model0→1, interop.publication→workspace.service0→13 once final counts stabilize and exact dependencies are recorded — independent journal completion and publication orchestration require existing workspace authority/checkpoint ports in an already established direction — cost if wrong: unnecessary coupling could be hidden, so independent task review must inspect exact new imports/calls and no unrelated baseline regeneration is permitted.

## D42 — Conditional publication

add the smallest editor-owned joined exact-checkpoint read that revalidates actual persisted workspace state and real Git head under the existing integration authority; retain ordinary editor.read isolation, independent journal completion verification and all HTTP/Git writes outside the transaction — this repairs a proved boundary defect — cost if wrong: an unchecked joined read could bypass state/authority or introduce lock-order problems, so actual-flow/concurrency regression and independent review must inspect the seam. No blanket transaction-policy change.

## D43 — Conditional publication

approve narrow portfolio-owned flush+refresh under actual scoped project/integration transaction, exposed through existing IntegrationPortfolioPort after typed requirement mutations and before contribution/snapshot — this freezes actual database values while preserving fingerprint meaning across dialects — cost if wrong: extra reads or lifecycle changes could affect locking/performance, so refresh only relevant requirement data, never clear context or alter ordinary reads, and require actual post-commit native Sync regression. No arbitrary rounding/dropped timestamp inputs. Recovery fault suite5/5 currentlypasses, fullTask 2notyetcomplete.

## D44 — Conditional publication

Schedule the independent Visio implementation while Publication Task 2 receives read-only review, before PublicationTask 3. Native/Word/frozen civilian graph inputs are already accepted; Visio does not consume conditional-publication state/API. Exactly one source/Maven owner remains (Visio), Task 2 reviewer is read-only. This removes idle time without weakening any gate or changing semantic scope. Cost if wrong: an unforeseen shared source dependency could invalidate a review; keep the Task 2 immutable diff, give Visio only its export/QA/docs scope, and serialize any subsequent Task 2 fix until Visio releases. Task 3 still consumes only accepted publication code.

## D44a — Accepted Task 2 lookup-only recovery correction

Accepted correction `55e21da4b2bf2021a2d87e43d6da0597c922ac73` makes UNKNOWN in
RECONCILIATION_REQUIRED use read-only receipt LOOKUP even when an earlier NOT_FOUND
allowed resubmission. Local movement must prevent a new SEND without preventing
later discovery of the original effect. A lookup-only NOT_FOUND ends that
invocation, retaining UNKNOWN and its reservation; it must not spin through the
100-attempt quota. FOUND can adopt the original receipt and expose reconciliation
once all uncertainty resolves. Reason: preserve recovery after real source movement
without inventing a new request/key or COMMON checkpoint. Risk: a missed guard
could resend into moved state, release the reservation too early or exhaust the
quota. Actual provider-latch, movement, persisted lease/attempt and one-effect
assertions cover those boundaries; normal valid-source resend and EXPIRED refusal
remain unchanged.

## D44b — Accepted Task 2 shared deadline and durable late Git result

The same accepted correction allocates one invocation budget at each public
preview/publish/retry/reconciliation entry and carries it through private
delegation. Remaining time is bounded by both wall-clock deadline and monotonic
elapsed time. Authorization and real local staging consume it; delegation cannot
grant a fresh 30 seconds, and expiry prevents scheduling new Git/HTTP effects.
Reason: a finite invocation contract must cover the whole call. Risk: inconsistent
clocks or renewed private budgets can extend execution or schedule effects late;
controlled-clock staging, accepted-plan and fetch-retry regressions verify it.

The existing deterministic editor checkpoint runs with explicit immutable
repository context, exact state and command metadata. Caller timeout neither
cancels nor joins the continuing writer; nonblocking executor cleanup preserves
the deadline. The editor's durable prepare/write/complete protocol retains late
completion, and later retry uses that same checkpoint identity. Old success or
deferred-failure adoption rechecks the current durable publication phase under
the connection transaction and cannot downgrade concurrent progress. Reason:
return within budget without abandoning a real Git effect. Risk: an asynchronous
writer could duplicate commits, lose context or overwrite newer progress; blocked
Git, durable late completion and overlapping stale-failure tests cover those
cases. No shared owner API, migration or dependency-baseline change was needed.

## D44c — Accepted fixture composition and warning disposition

Task 2's correction explicitly supplies application and test-configuration classes
instead of ambiguous inherited Spring default discovery. The named configuration
warning disappears without suppressing diagnostics. Reason: make the genuine
application/provider fixture composition deterministic. Existing HSQL/Flyway,
explicit-dialect, Lucene/JDK, bootstrap credential-path and immutable-relationship
warnings remain inherited and nonblocking; no connected correction failure was
established. Passing tests do not imply warning-free output. Risk: broad warning
suppression or unrelated cleanup would hide evidence or expand scope, so neither
was done.

## D44d — Accepted correction evidence and unresolved R1 history

Independent scoped spec/quality review approved R1, R2 and the fixture correction
at `55e21da4b2bf2021a2d87e43d6da0597c922ac73`; 34 final tests passed with no failures,
errors or skips (flow 9, recovery 19, partial 4, concurrency 1, actual two-client-JVM
restart 1). Four source and twenty evidence hashes were controller-verified.
The old restart provider lived inside the client process; Task 3's separate
provider/client matrix is additional evidence, not a retroactive description.

The first corrected six-test run had one R1 assertion failure, followed by an
isolated diagnostic pass on unchanged production source. Prints executed after
the recovered result cannot explain the pass; earlier compiled bytes and durable
lease/phase detail were not captured. **That original failure remains unexplained.**
A single controlled clock then replaced mixed artificial-future and system-clock
assumptions, with explicit persisted lease clearing, UNKNOWN/resubmit state and
exact attempts. The final passing tests and current control flow support
acceptance; they do not establish the historical cause. Reason: preserve both
accepted evidence and uncertainty before temporary logs are removed. Risk:
retelling a later pass as an explained fix would conceal a remaining historical
blind spot. The original separately recorded TDD deviation (D69) also remains.

## D45 — Conditional publication

Task 3 may add bounded sanitized accepted PublicationReview/resolutions, frozen PublicationScope, expectedExternalRevision and requestFingerprint to the public PublicationOperation projection, with source-compatible constructor and explicit additive schema1 meaning. Project only the existing authorized persisted review/request; retain existing limits and omit leases/raw requests/credentials. Existing authorized generic Operation GET supplies original InternalState/context. Retry remains operation-ID-only and never accepts edited form values into an existing frozen request. Reason: the current projection cannot restore directed decisions or model/package scope after reload, especially failed-fetch preview=null. Cost if wrong: accidental authority/secret exposure or compatibility break; route/reload/security/bounds tests and independent review are mandatory.

## D46 — Conditional publication

Task 3 may accept optional bounded rootResource/selector availability inputs on existing Overview, constructing ExternalScope from the authorized connection. Missing scope stays fail-closed, while a known unverified PCS adapter retains the concrete guarantees-unverified reason rather than implying that selecting scope grants writes. UI describes actual model/package/subset, not an invented digest; selector retains exact adapter meaning. Expected external revision remains required for preview. Reason: availability is scope-specific and cannot safely infer scope from legacy overview. Cost if wrong: misleading capability UI or scope escape; wrong-scope/direct-request/default-denial tests must preserve server-owned authority.

## D47 — Conditional publication

preserve the real atomic plan/local-stage transaction in publication tests. Source inspection of IntegrationPublicationService.publish shows beginPublication and stagePublicationLocal (including local apply) commit in one existing store/local transaction; there is no separately reachable committed-plan/pre-local-apply window to manufacture. Test the actual accepted-plan commit with a Push requiring no local change before dispatch, and the actual Sync local-apply commit before Git; explain coincident boundaries explicitly. Do not split the product transaction or seed fake accepted journal state merely to obtain nine differently named windows. Reason: process-restart evidence must follow real durability boundaries and preserve atomic semantic state. Cost if wrong: an actual independent persistence boundary could be missed; test decorators must observe committed state and the matrix must identify exact transaction/phase evidence.

## D48 — Conditional publication

additional actual restart/security coverage may initially pass an already correct engine; never manufacture a missing-harness failure or call it behavioral RED. New API/UI behavior still requires meaningful missing-feature RED. Reason: avoid repeating the earlier misclassified fixture-first failure while preserving TDD for implementation. Cost if wrong: a new implementation seam could lack a meaningful failing invariant; independent review checks each claimed new behavior and fault assertion.

## D49 — Conditional publication

approved additive publication review/reload projection must be budgeted before the first local effect in the existing journal/projection path. Concrete inspection confirms the aggregate16 MiB public-operation budget combines preview, item receipts, completion and now accepted review; a late projection exception could otherwise leave an unreadable operation after effects. Use frozen known identities/target plus conservatively bounded remaining receipt fields, preserve the cap and prove near-budget refusal with zero effects. Do not reserve16 MiB per receipt or redesign pagination speculatively. Reason: keep valid durable progress readable under the same bounded API contract. Cost if wrong: conservative accounting may reject extreme legal combinations earlier; report exact limits, and escalate a separate remote-metadata completion problem only when a concrete unreadable-state/COMMON effect is proved.

## D50 — Conditional publication

Stop the 900-command budget fixture when it stalls before publication; retain exit 130 as a setup failure. Use a focused real journal/projection case that independently proves the aggregate limit and unchanged effect counters. This avoids expensive unrelated setup. Risk: narrower coverage could miss service ordering, so retain real journal guards and normal end-to-end tests; no mock success, raised bounds or performance side project.

## D51 — Conditional publication

Commit the genuine browser-ready path after focused API/budget verification, before finishing the complete process matrix, so real CI can run in parallel. The browser may reuse the test HTTP provider, but independent provider/client JVMs remain mandatory for restart evidence. Risk: later changes can invalidate captures; retain exact sources, inspect actual images and run final exact-tree CI. This ordering does not authorize partial completion or merge.

## D52 — Conditional publication

Reject an explicitly different requested branch with 409 through one shared publication HTTP boundary check. Select publication retry/cancel/endpoint behavior by persisted direction; keep WorkspaceContextResolver, absent-branch handling and legacy Pull/file behavior unchanged. The observed 200 came from ignoring the query parameter, not an unauthorized write. Risk: broad checking could break valid legacy/reload calls; regress the mismatch, unchanged operation and zero additional provider effects.

## D53 — Conditional publication

CI 35524367930 passed, but all ten new images were inspected and only nine showed the required state. Reframe the EN package image and execute enabled reconciliation with EN/DE captures. The combined partial+UNKNOWN image is sufficient; no redundant image is required. Use a real local change/checkpoint → Push → explicit SKIP/PARTIAL → linked preview. Risk: irrelevant screenshots create false confidence, so inspect corrected actual CI bytes before documenting acceptance.

## D54 — Conditional publication

The document-template CI failed the unchanged horizontal-overflow assertion at EN 390×844 saved-diff; DE 1280×900 passed. Publication CSS is not loaded on that page, and the artifact has no exact overflowing-node dump. Add only bounded viewport/scroll-width/selector/rectangle/computed-style diagnostics and a real failure image in the existing failure path. Keep the guard unchanged. Risk: another CI round trip costs time, but guessing shared CSS or weakening the guard would conceal the defect. Civilian Word/Visio export acceptance remains separate.

## D55 — Conditional publication

Approve only interop.controller → workspace.service dependency count 5→6. Three actual database lanes each had 2,285 app tests with that sole ratchet failure; the focused run had 23 functional passes. The new class reference is the authorized RepositoryContext used by the approved branch comparison, with persisted direction selection and no resolver-authority change. Risk: a broad baseline update could hide coupling; change only this entry, require 22 ratchet passes and final real database CI.

## D56 — Visio presentation

Keep the pre-existing 32 MiB aggregate package budget in addition to 32 pages, 20k nodes and 60k connectors. The original wording said XML; D73 corrects it to all uncompressed package parts. Large in-memory models already exceeding this budget were never valid packages. Optional detail pages must fit remaining capacity without rejecting an otherwise valid base export. Risk: inaccurate accounting could reject valid input or weaken parsing bounds; require near-limit regression and actual export QA.

## D57 — Visio presentation

Derive generated detail-caption truncation losses while the manifest owner enumerates retained optional pages; preserve source/base losses. The accepted fixture has no relevant truncation and is only 1,067,494 uncompressed bytes, so retain all eleven reviewed images with their original 9e0fb1b source identity. Risk: an unrecognized in-memory consumer or changed fixture could lose evidence; inspect consumers and test long types and capacity trimming before scoped review.

## D58 — Cross-slice clarification

The final Word wave and seven-finding correction received complete 116-page local visual review. The published f7521d833d8617fb564aa3b4441c1edba1859e20 tree matched that source. Exact-tree CI remained its merge gate; combined local clean-root verification runs once after all feasible slices in an isolated worktree. This avoids duplicate whole-root work while retaining final interaction coverage.

## D59 — Cross-slice clarification

Plan deletion and native types only from accepted normalized artifacts. Reject concurrent endpoint/mapping type disagreement before effects. A later explicit remap must update native semantics and retained/exported evidence consistently, with export/reimport idempotence. Otherwise review choices could silently alter meaning.

## D60 — Cross-slice clarification

Visio presentation uses upright connector text, deterministic collision handling, strokes behind nodes and bounded native detail pages of at most six endpoints/six relations. Preserve full overview/optional impact, semantics and glue. Keep existing page/shape/connector budgets and explicit capacity disposition; cover all 44 civilian relations with readable captions and inspect changed Draw pages. Proprietary Visio/EA execution remains unavailable.

## D61 — Cross-slice clarification

Complete all three publication tasks; a DTO-only delivery is insufficient. Start from accepted Word/native foundations and allow the independently deliverable native PR to finish its own exact-tree CI while publication proceeds. This reduces waiting without dropping generic architecture or proprietary-product limits; final combined verification still guards interactions.

## D62 — Cross-slice clarification

PR1089 now retains Word1088 HEAD as parent and targets main for mandatory CI. Use ordinary merge commits in dependency order (Word then native) where repository policy permits; repository settings allow this and prior stacked-merge plan retained ancestry. Never force-push or bypass protected-branch gates. Verify expected source SHA and computed merge tree. This supersedes the earlier generic squash example for this actual dependent stack.

## D63 — Cross-slice clarification

GitHub merge API rejected ordinary merge for1088 with405 “Merge commits are not allowed on this repository,” despite earlier repository settings. Use supported squash merge with exact expected source SHA and preserve dependent branch state by incorporating the new main ancestry in a non-force feature-branch commit if necessary — the user's merge authorization persists, and repository policy must be honored — cost: an additional tested branch synchronization commit and revised source-history links; no source-tree change, force push or protected-branch bypass is permitted.

## D64 — Cross-slice clarification

Finish the native semantic plan with its own final broad review after the concrete CI fixes on its isolated source, so PR1089 can merge when that review and its exact-tree CI pass. The later publication/Visio broad review starts from this accepted foundation and reviews new cross-feature interactions rather than auditing unchanged native code again — the user explicitly requested reviewed green PRs to be merged, and native semantic behavior is independently deliverable — a later integration defect remains possible, so the final combined clean reactor and actual CI contracts remain required. All native deferred minors and the diagnosed CI fixes belong in this native final review.

## D65 — Cross-slice clarification

allow an immutable server-owned exact connector/profile-version native projection registration in the existing adapter, preserving current Sparx-v2 defaults and separating projection from verified publication authority. Reason: the genuine neutral HTTP provider must exercise actual native Sync without impersonating Sparx. Cost if wrong: accidental native projection for another connector; exact registration/default-denial/version tests are required.

## D66 — Cross-slice clarification

reuse accepted complete Word visual evidence for the unchanged foundation; changes to render paths/assets/templates/graph/layout/fixture trigger fresh relevant visual checks, and final source/CI gates remain mandatory. Reason: avoid repeating unchanged optional tests. Cost if wrong: an unrecognized rendering dependency could leave stale visual evidence; inspect dependencies before reuse.

## D67 — Cross-slice clarification

require narrow actual requirement-mapping cross-JVM replay evidence in the native final correction because the binding native spec requires package/mapping idempotence across restart. Reason: the prior process fixture asserted only packages/elements. Cost if wrong: additional bounded test maintenance/runtime; existing real ports/fixtures are reused. This is now implemented and independently accepted (native final C3).

## D68 — Cross-slice clarification

dedicated native/package screenshots remain the existing publication Task 3 overall-completion gate, while exact-head EN/DE/keyboard browser execution remains the native PR merge gate. Reason: native Task 2/spec requires that execution, and the separate screenshot deliverable was explicitly assigned to Task 3. Cost if wrong: later visual QA could find layout work still necessary before the overall completion. No missing screenshot is claimed as inspected.

## D69 — Cross-slice clarification

retain the publication implementation authored after misclassified fixture-only RED; record the deviation, correct the real transaction fixture, capture a controlled missing-engine stub baseline, restore exact hashed source and verify real fault invariants. This is not retroactive RED-first proof. Reason: retyping code adds no verification value. Cost if wrong: post-hoc tests may share implementation blind spots; independent real-provider/state assertions, full required fault coverage and explicit reviewer handoff are mandatory.

## D70 — Cross-slice clarification

A native final-head test exposed an uncontrolled normal-load heap assumption. Preserve the real pressure guard and eight-root contract; control only ambient samples in method-scoped synchronous tests and add emergency-response coverage. Scope the correction and review narrowly. Risk: unscoped control could mask asynchronous behavior, so existing guard tests remain.

## D71 — Cross-slice clarification

Publication Task 2 may adjust only the three explicitly reported existing-direction workspace dependency entries after final compiled counts, with dependency justification and independent review. No new module direction or broad baseline regeneration.

## D72 — Cross-slice clarification

The real-flow self-block required an editor-owned joined exact-checkpoint read while preserving ordinary REQUIRES_NEW isolation. Portfolio-owned scoped flush/refresh freezes actual persisted timestamp precision without changing fingerprint inputs. Both narrow seams require actual-flow regression and independent review; broad transaction changes or arbitrary timestamp rounding would weaken state authority.

## D73 — Cross-slice clarification

Clarification of the preceding Visio byte-limit ruling: source inspection establishes the unchanged 32 MiB bound over aggregate uncompressed package-part bytes, including the manifest, not XML alone. The earlier XML shorthand is superseded. Exact UTF-8 part accounting and the pre-existing validator remain authoritative; only explicitly marked optional detail pages may be trimmed. Near-limit regression and final actual HTTP/Draw checks passed; independent source review remains pending.

## D74 — Cross-slice clarification

permit Task 3's fresh implementer to perform read-only preflight while Task 2 fix receives independent review and Visio fix owns the sole source/Maven slot. No code/tests/Git/build/remote actions are allowed until accepted Task 2 and explicit owner transfer. Reason: API/browser/restart handoff is already specified; identifying exact consumer/seam questions now removes idle time without accepting pending service behavior. Cost if wrong: a Task 2 review correction could invalidate a preflight assumption; final START_BASE and accepted handoff must be rechecked before edits.

## D75 — Cross-slice clarification

All eleven Visio pages from CI 35524367930, rendered by LibreOffice 24.2.7.2, were inspected: all 44 direction/type captions remain readable, with dense-overview, centered-arrowhead and whitespace limits explicit. No new render was run. The Word graph remains 5365c5d5fcae616a37e84ed3be15f3c62a2ad658a35e60c0bf2568dfc7f7c191, with 68+11 pages, 344/85 checks, no empty body pages and zero extra LLM calls. Seventeen export and 90 page-image hashes were checked; unchanged Word visual acceptance is reused with its honest source.

## Final capture publication and CI disposition

The corrected capture source `dfc0de97f9b43089ada325c9b1a191134b9b7876` was
published without force as `4109815b6c045ee37aa4b6cb3588baaeb68e4c5e`; both have
tree `7c1ecbbf6aa564013f7469aff061d2305fbd7c69`. Fifteen source and 51 evidence
hashes were independently checked. The initial core CI had 2,294 app tests with
one 5→6 ratchet failure and no errors; each database lane had 2,285 with that same
sole failure. The aggregate's draft-PR guard failure is intentional and is not
classified as a second product defect. New exact-tree CI remains mandatory after
the reviewed baseline correction. Reason: distinguish actual failures, intentional
release policy and verified fixes; conflating them would misstate acceptance.

## D76 — Bounded URI-search review correction

Securitybot review 5261139660 (alert 87) flagged the wildcard regular expression in
`PublicationBounds.resource`. The existing `text()` guard already rejects inputs
longer than 2,048 characters; no unbounded-input exploit or runtime failure was
reproduced. Replace the search with a precompiled fixed-length case-insensitive
pattern and `find()`, without wildcard repetition, suppression or raised bounds.
Preserve all seven blocked encodings, URI checks and sanitized diagnostics. The
focused compatibility/planner/receipt selection passed 35 tests. This is
semantics-preserving hardening, not a fabricated performance RED. Risk: changed
encoded-delimiter acceptance could weaken URI validation; the case, position,
2,048/2,049 boundary and legal-escape regressions cover that contract. Independent
review and publication precede resolving the remote thread.

## D77 — First-measurement overflow guard

The second actual document CI run passed EN 390×844 and DE 1280×900, and produced
no overflow diagnostic files. Thus the first measurement already passed; the
controller also viewed the actual mobile saved-diff image. The original failure's
cause remains unproved. Freeze the first viewport measurement in a boolean and
assert that exact value after writing optional failure diagnostics. A later layout
change during diagnostic capture must never turn an initial failure into a pass.
Keep the original tolerance and add no wait, CSS change or suppression. Syntax
verification plus mandatory final real CI are proportionate to this narrow guard
preservation. Risk: asynchronous layout can still fail legitimately; retain the
bounded offending-node evidence rather than guess a CSS correction.

## D78 — Corrected actual screenshot acceptance

Civilian CI 35525893725 produced all fourteen required EN/DE native/publication
images. The controller inspected every image and accepted complete package
controls, endpoint review, directed decisions, partial UNKNOWN, recovery, enabled
reconciliation and the actual successor with predecessor link. Copy exactly those
bytes with source/tree/artifact/image hashes; retain the earlier wrong-viewport
and missing-reconciliation findings as historical evidence. Reason: screenshots
must prove real completed actions at a named source, not just a green browser run.
Risk: later UI changes could invalidate them; final exact-tree CI remains required,
and changed capture inputs require new relevant visual review.

## D79 — Editable review versus ambiguous publication submission

Task 3 review found that a cached rejected review survived bulk/rationale edits and
could target a different displayed preview. Keep only ephemeral pending submissions
bound to connection, operation and request/preview fingerprints. A missing POST
response or failed status read is not rejection: freeze all review choices and
publication until the existing authorized status GET resolves uncertainty. A
confirmed unaccepted PREVIEWED operation with REVIEW/PUBLISH becomes editable and
the next submission uses its current visible decisions; accepted review comes from
the server and retry stays operation-ID-only. Operation/connection selection guards
ignore late unrelated responses. Reason: the submitted directed review must match
the visible review without replacing possibly accepted keys. Risk: premature
invalidation could alter an ambiguous operation, while indefinite stale caching
rejects corrected intent; focused rejected/bulk/rationale/switch/ambiguous/accepted
and asynchronous-response DOM paths cover both. No server or journal change and no
new durable browser authority is introduced.

## D80 — Late receipt authority and joined exact-checkpoint proof

PR 1094's service review exposed narrower defects than a blanket stale-lease ban:
old nonterminal no-effect evidence could resolve a newer SEND, and newer uncertainty
callbacks could erase a validated terminal rejection. Retain obsolete nonterminal
receipts only in their attempt history; preserve terminal receipt authority across
leases while retaining exact request/sequence checks and owner/epoch lease release.
Reason: NOT_FOUND cannot fence an overlapping invocation, so lost-response recovery
must still LOOKUP without losing legitimate late APPLIED or rejection evidence.
Risk: discarding all historical receipts would prevent safe recovery; real HTTP
and durable-lease tests cover both rejection preservation and late APPLIED adoption.

The public exact-checkpoint helper now samples authoritative Git HEAD after its
existing joined workspace row lock. The live publication caller already held that
lock; no live-publication bypass is claimed. A real concurrent version Git commit
followed by ORM rollback reproduces the direct helper's former stale true result.
No new lock order, REQUIRES_NEW, schema, port or external-Git fence is introduced.
Four new failure regressions reached meaningful RED before correction; one initial
CLOB fixture error is retained separately. Seven focused tests and the final 41-test
Flow/Partial/Concurrency/Recovery/editor selection passed with no failures or skips.
Independent service-fix review and new-source final CI remain pending.


## D81 — Final I1–I3 preservation and recovery correction

Preserve the Sparx model-root occurrence exception for legacy v1 independently of
native package-edit capability. Registered neutral profiles and Sparx v2 retain
their existing root semantics; v1 still imports packages as retained evidence.
Reason: the v1 reader encodes hierarchy with model-root containers and parent
placement IDs. The regression uses that actual reader and integration export,
checking all retained IDs, parents, sibling positions and absence of false losses.

Keep the receipt contract's 100-character safe failure code and complete validated
receipt JSON. Only scalar item/operation/event diagnostics longer than 64 characters
map to the existing receipt-state name. Reason: legitimate terminal evidence must
not roll back into UNKNOWN because a diagnostic column is shorter. Real HTTP SEND
and lost-response LOOKUP cases cover 64, 65 and 100, durable full receipt equality,
terminal projection and an explicit successor. Internal safe-code guards remain.

Resolve a RETRYABLE_NO_EFFECT item for explicit reconciliation only when every
persisted SEND for it has its own completed validated no-effect receipt. Neither a
timeout's ended timestamp nor a LOOKUP receipt proves an older SEND cannot commit.
Keep that uncertainty reserved and recoverable through read-only LOOKUP after
local movement. A repeated nonterminal FOUND ends the invocation and releases only
its owner/epoch lease; it cannot consume the 100-attempt quota in a loop. Terminal
receipts remain authoritative, and movement never permits a new stale-plan SEND or
premature COMMON. The real overlapping-provider case records a timeout, retains
its unresolved SEND after a newer no-effect and repeated lookup, then adopts the
older committed-but-lost result through lookup: one mutation, one frozen key/request.

Five initial behavioral failures across six executions preceded production edits;
the first focused GREEN passed all six. The later strengthened SEND/LOOKUP and
timeout cases were explicitly tested with the journal/service restored to BASE:
eight executions, six expected failures, two passing 64-character controls. An
intermediate run executed stale test bytecode and is retained separately, excluded
from that strengthened RED claim. Its cause is not established; removing affected
generated compilation outputs restored the correct signature and assertions.
The original GREEN production hashes were restored exactly. Final covering
validation passed 60 tests (14 interop, 46 app), with no failures/errors/skips;
inherited compiler/JVM/HSQL/Flyway/Hibernate/Lucene notices remain non-pristine.

No schema, public API, dependency, ratchet, browser asset or export-rendering input
was changed. The existing EN/DE help and contract spec describe these precise
recovery and diagnostic rules. Fresh scoped rereview and controller exact-source
clean-root/required CI remain gates; prior browser/process/CI evidence is historical.
Actual EA/PCS/SBPI/MS Word/MS Visio remain NOT_EXECUTED. Process-crash evidence does
not establish power-loss durability or arbitrary external Git-writer fencing.
