/**
 * Shared ArchiMate exchange codecs, identities and normative schema validation.
 *
 * <p>Import and export adapters depend on this format boundary; these classes
 * depend only on neutral diagram/model contracts and must not depend on the
 * application export, catalog or analysis services.</p>
 */
package com.taxonomy.archimate.exchange;
