# Sparx fixtures

`semantic-model.xmi` is a synthetic XMI 2.1 contract fixture authored for this
repository (MIT). Its exporter label describes the extension syntax being tested;
it was not generated by Enterprise Architect. No real-product compatibility or
specific EA version is established by this fixture.

It exercises packages, typed elements, a requirement, tagged values, a dependency
and excluded diagram geometry. Product-generated fixtures may be added only with
documented source, product version, export settings and redistribution permission.
