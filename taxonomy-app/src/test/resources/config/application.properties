# The canonical test suite deliberately exercises semantic and pinned-ONNX paths.
# Production and container defaults remain disabled and download-denied unless an
# operator explicitly opts in.
embedding.enabled=true
embedding.allow-download=false
